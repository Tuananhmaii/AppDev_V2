@model IEnumerable<Order>
@foreach (var item in Model){
    <h3>@item.Id</h3>
    <h3>@item.Name</h3>
    <h3>@item.PhoneNumber</h3>
    <h3>@item.City</h3>
    <h3>@item.Address</h3>
    <h3>@item.TotalPrice.ToString("c")</h3>
    <a asp-action="Details" asp-route-orderId="@item.Id">Details</a>
}